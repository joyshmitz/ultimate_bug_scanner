name: UBS Manifest

on:
  push:
    branches: ["master"]
  pull_request:

jobs:
  manifest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y python3 jq ripgrep >/dev/null
      - name: Run UBS manifest suite
        env:
          NO_COLOR: "1"
        run: |
          cd test-suite
          ./run_manifest.py --fail-fast
